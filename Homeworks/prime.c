#include <stdio.h>
#include <math.h>
#include "check_prime.h"

int find_square_root(int num)
{
    return (int)sqrt((double)num);
}

void main ()
{
    int *array, length, ctr, maxprime;

    for (ctr=0; ctr<length; ++ctr) {
        if (isprime(array[ctr])) {

        }
    }
}
